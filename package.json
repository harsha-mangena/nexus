{
  "name": "@nexus/root",
  "version": "0.1.0",
  "private": true,
  "description": "Nexus — Privacy-first, self-hosted personal AI assistant",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @nexus/app dev",
    "start": "pnpm --filter @nexus/app start",
    "setup": "pnpm --filter @nexus/cli start",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2"
  }
}
